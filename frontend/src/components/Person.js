const Person = ({person, handleDelete})=><div>
        
    {/* {filteredPerson.map(person=> */}
    <div >{person.name} {person.number}
    <button onClick={handleDelete}>delete</button>
    </div>
    {/*)}'*/}
    </div>

    
export default Person